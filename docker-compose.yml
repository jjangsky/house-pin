services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: housepin
      POSTGRES_USER: housepin
      POSTGRES_PASSWORD: housepin
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
